Both Classifiers take in 20x20 pixel images of labeled hand-written digits.

A dot product Kernel is used to classify the images by comparing 2 digits at a time. (i.e. is this image a one or a two?)

How well does linear kernel do vs quadratic kernel?
